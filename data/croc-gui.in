#!/usr/bin/env python3
# Launcher generated by Meson
import sys

# Allow running the installed modules
try:
    from croc_gui import main
except ImportError:
    # Fallback if layout differs
    sys.path.insert(0, '@PKGDATADIR@')
    from croc_gui import main

if __name__ == "__main__":
    main()
