#!/usr/bin/env python3
# Launcher generated by Meson
import os
import sys

os.environ.setdefault("GATOR_LOCALEDIR", "@localedir@")

# Allow running the installed modules
try:
    from gator import main
except ImportError:
    # Fallback if layout differs
    sys.path.insert(0, '@PKGDATADIR@')
    from gator import main

if __name__ == "__main__":
    main()
